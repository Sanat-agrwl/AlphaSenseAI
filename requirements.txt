# ─── Core ────────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
loguru>=0.7.0
pandas>=2.0.0
numpy>=1.26.0
pyarrow>=14.0.0          # parquet read/write

# ─── Data ────────────────────────────────────────────────────────────────────
requests>=2.31.0
feedparser>=6.0.10       # RSS feed parsing
nsepython>=2.0           # NSE helper (optional, fallback to direct API)

# ─── NLP / Sentiment ─────────────────────────────────────────────────────────
transformers>=4.40.0
torch>=2.2.0             # CPU build fine for inference; GPU optional
datasets>=2.18.0         # for fine-tuning
accelerate>=0.27.0       # HuggingFace Trainer acceleration

# ─── Audio ───────────────────────────────────────────────────────────────────
openai-whisper>=20231117
pyannote.audio>=3.1.0    # speaker diarization (optional)
# ffmpeg must be installed via: brew install ffmpeg

# ─── Signal & Backtest ───────────────────────────────────────────────────────
scipy>=1.12.0            # for rolling z-score / stats

# ─── Broker ──────────────────────────────────────────────────────────────────
kiteconnect>=5.0.1       # Zerodha Kite Connect (only needed for live mode)

# ─── Dashboard ───────────────────────────────────────────────────────────────
streamlit>=1.33.0
plotly>=5.20.0

# ─── Dev / Testing ───────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-cov>=5.0.0